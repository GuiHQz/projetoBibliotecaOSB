import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles ({
    lupa: {
        color: "#FFF"
    },

    input: {
        position: "relative",
        top: "12%",
        transform: "translate(-25%, -25%)",
        borderRadius: "3px",
        borderStyle: "1",
        borderColor: "black"
    }
})